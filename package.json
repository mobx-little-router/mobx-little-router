{
  "private": true,
  "version": "independent",
  "repository": "git@github.com:jaysoo/mobx-router.git",
  "author": "Jack Hsu <jack.hsu@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build --stream",
    "publish": "lerna run publish --independent",
    "test": "lerna run test --stream"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "lerna": "^2.0.0"
  }
}

// @flow
import { EventTypes, install } from '../index'
import takeUntilEvent from './takeUntilEvent'

describe('takeUntilEvent', () => {
  test('resolves when an event is seen from router', () => {

  })
})
